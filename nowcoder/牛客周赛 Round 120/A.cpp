#include <bits/stdc++.h>

using namespace std;

using i32 = int32_t;
using i64 = long long;

#define int i64

const int inf = INT_MAX / 2;

i32 main() {
    ios::sync_with_stdio(false), cin.tie(nullptr);
    int n;
    cin >> n;
    for (int i = 0; i < n; i ++) cout << 'a';
    cout << 'b';
    for (int i = 0; i < n; i ++) cout << 'a';
    return 0;
}
